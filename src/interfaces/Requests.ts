import {Request} from "express";import {ParamsDictionary} from 'express-serve-static-core';export interface RequestWithBody<T> extends Request {    body: T}export interface RequestWithPathParams<T extends ParamsDictionary> extends Request {    params: T}export interface RequestWithPathParamsAndBody<T extends ParamsDictionary, K> extends Request {    params: T    body: K}